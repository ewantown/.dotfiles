;; -*- lexical-binding: t -*-
;; WSL Configuration
;==============================================================================
;; stub
(defun init-wsl (conf stem)
  "Initialize on Windows Subsystem for Linux"
  "not yet implemented")
;==============================================================================
(provide 'wsl-config)
